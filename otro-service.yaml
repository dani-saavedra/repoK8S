  apiVersion: v1
  kind: Service
  metadata:
    name: estudiante
  spec:
    type: ClusterIP
    selector:
      app: estudiante
    ports:
      - protocol: TCP
        port: 80
        targetPort: 8080
