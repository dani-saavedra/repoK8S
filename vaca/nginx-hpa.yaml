apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: vaca
  #namespace: npe
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: vaca
  minReplicas: 2
  maxReplicas: 2
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 80

# Requests lo vamos a calcular con el promedio de las peticiones por minuto. no con el valle y no con el pico.
